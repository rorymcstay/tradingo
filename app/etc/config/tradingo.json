{
    "connectionString": "${CONNECTION_STRING}",
    "baseUrl": "${BASE_URL}",
    "apiKey": "${API_KEY}",
    "apiSecret": "${API_SECRET}",
    "marketdata-topics": [
        "quote:${SYMBOL}",
        "trade:${SYMBOL}",
        "instrument:${SYMBOL}"
    ]
}
