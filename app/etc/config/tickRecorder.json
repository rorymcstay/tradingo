{
    "connectionString": "wss://www.bitmex.com",
    "baseUrl": "https://www.bitmex.com/api/v1/",
    "shouldAuth": false,
    "logFileLocation": "${STORAGE}/log/",
    "storage": "${STORAGE}",
    "symbol": "${SYMBOL}",
    "libraryLocation": "${INSTALL_LOCATION}/lib/libtest_trading_strategies.so",
    "marketdata-topics": [
        "quote:${SYMBOL}",
        "trade:${SYMBOL}",
        "instrument:${SYMBOL}"
    ]
}
