{
    "storage": "${STORAGE}/${RUN_ID}",
    "logFileLocation": "${STORAGE}/${RUN_ID}/log",
    "logLevel": "${LOG_LEVEL}",
    "libraryLocation": "${INSTALL_LOCATION}/lib/libtrading_strategies.so"
}
